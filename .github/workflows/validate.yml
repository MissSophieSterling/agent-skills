name: validate
on:
  push:
  pull_request:
jobs:
  skills:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Check every SKILL.md and the umbrella bundle
        run: python3 tools/validate.py
